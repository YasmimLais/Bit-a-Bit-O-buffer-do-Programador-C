#include <stdio.h>
#include <string.h>

int main(){

    char palavra[101], vetor[101];

    scanf("%100[^\n]", &palavra);//todo caract

    printf("%s\n", palavra);
    printf("%d\n", strlen(palavra));  // Tamanho escrito - \0
    
    int j = 0;
    
    for(int i = 0; i <= strlen(palavra); i++ ){  
        
        if (palavra[i] == ' '){
            char vt[101];
            int y = 0;
            for (int k = i + 1; k <= strlen(palavra); k++){
                
                if (palavra[k] == ' '){
                    if(strlen(vetor) == strlen(vt)){
                        if (vetor[0] == vt[0]){


                        } 
                    }
                    strcpy(vt, "");
                    y = 0;
                }

                vt[y] = palavra[k];
                y++;
            }
            
            strcpy(vetor, "");
            j = 0;
        }
        vetor[j] = palavra[i]; 
        j++;
    }
    
    return 0;


}
